MovieBuffs API Gatling Perf Tests
=========================

To test it out, simply execute the following command:

    $ ./mvnw gatling:test -Dgatling.simulationClass=moviebuffs.MovieBuffsAPISimulation

or simply:

    $ ./mvnw gatling:test
